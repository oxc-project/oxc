name: Release NAPI React Transform

permissions: {}

on:
  push:
    branches:
      - main
    paths:
      - napi/transform-react/package.json
      - .github/workflows/release_napi_transform_react.yml
      - .github/workflows/reusable_release_napi.yml

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  release:
    if: github.repository == 'oxc-project/oxc'
    name: Release NAPI React Transform
    uses: ./.github/workflows/reusable_release_napi.yml
    with:
      name: transform-react
    permissions:
      id-token: write # for `pnpm publish --provenance`
