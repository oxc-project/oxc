ignore-interior-mutability = ["oxc_linter::rule::RuleWithSeverity"]

disallowed-methods = [
  { path = "str::to_ascii_lowercase", reason = "avoid a memory allocation from `to_ascii_lowercase`" },
  { path = "str::to_ascii_uppercase", reason = "avoid a memory allocation from `to_ascii_uppercase`" },
]
