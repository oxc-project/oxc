export const server_settings: { host: string; port: number; retries: number } = {
  host: "localhost",
  port: 8080,
  retries: 3,
};

export const greeting: { text: string; loud: boolean } = {
  text: "hello",
  loud: false,
};
