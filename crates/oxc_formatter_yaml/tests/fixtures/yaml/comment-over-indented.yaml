# DIVERGENCE: a comment indented deeper than the value it follows never
# rewrites that value's layout; Prettier breaks `key: value` onto two lines
# (`key:\n  value`) when such a comment follows it.
Properties:
  Type: application
    # over-indented comment
  Other: 1
