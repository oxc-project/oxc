query: |
  query {
    File {
      type_annotation {
        type {
          str @filter(op: "=", value: ["$Number"])
          span_: span {
            start @output
            end @output
          }
        }
      }
    }
  }

args:
  Number: "Number"

reason: Use number instead