query: |
  query {
    File {
      type_annotation {
        type {
          str @filter(op: "=", value: ["$String"])
          span_: span {
            start @output
            end @output
          }
        }
      }
    }
  }

args:
  "String": "String"

reason: Use string instead