export default {
  rules: {
    "no-debugger": {
      create(context) {
        return {
          DebuggerStatement(node) {
            context.report({ message: "Unexpected Debugger Statement", node });
          },
        };
      },
    },
  },
};
