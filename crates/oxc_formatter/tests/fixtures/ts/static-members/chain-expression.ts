this._position = this._editor
  ._getViewModel()
  ?.getPrimaryCursorState().modelState.position;

this._position = this._editor
  ._getViewModel()
  .longlonglonglonglonglonglong()
  ?.getPrimaryCursorState().modelState.position;
