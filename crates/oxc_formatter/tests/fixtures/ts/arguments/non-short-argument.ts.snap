---
source: crates/oxc_formatter/tests/fixtures/mod.rs
---
==================== Input ====================
setTimeout(
  () => {
    //
  },
  timeout * Math.pow(1)
);

setTimeout(
  () => {
    //
  },
  true || isFinite(Infinity)
);

setTimeout(
  () => {
    //
  },
  call(f) as any
);

setTimeout(
  () => {
    //
  },
  call(f) satisfies any
);

==================== Output ====================
------------------
{ printWidth: 80 }
------------------
setTimeout(
  () => {
    //
  },
  timeout * Math.pow(1),
);

setTimeout(
  () => {
    //
  },
  true || isFinite(Infinity),
);

setTimeout(
  () => {
    //
  },
  call(f) as any,
);

setTimeout(
  () => {
    //
  },
  call(f) satisfies any,
);

-------------------
{ printWidth: 100 }
-------------------
setTimeout(
  () => {
    //
  },
  timeout * Math.pow(1),
);

setTimeout(
  () => {
    //
  },
  true || isFinite(Infinity),
);

setTimeout(
  () => {
    //
  },
  call(f) as any,
);

setTimeout(
  () => {
    //
  },
  call(f) satisfies any,
);

===================== End =====================
