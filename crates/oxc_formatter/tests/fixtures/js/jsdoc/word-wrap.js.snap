---
source: crates/oxc_formatter/tests/fixtures/mod.rs
---
==================== Input ====================
/**
 * This is a very long description that should be wrapped because it exceeds the maximum line width that is configured for the formatter to produce nice output.
 */
function longDescription() {}

/**
 * @param {string} name - This is a parameter with a very long description that should be wrapped because it exceeds the maximum line width.
 */
function longParamDesc(name) {}

/**
 * Short.
 */
function shortDesc() {}

==================== Output ====================
-------------------------------
{ jsdoc: true, printWidth: 80 }
-------------------------------
/**
 * This is a very long description that should be wrapped because it exceeds the
 * maximum line width that is configured for the formatter to produce nice
 * output.
 */
function longDescription() {}

/**
 * @param {string} name - This is a parameter with a very long description that
 *   should be wrapped because it exceeds the maximum line width.
 */
function longParamDesc(name) {}

/** Short. */
function shortDesc() {}

--------------------------------
{ jsdoc: true, printWidth: 100 }
--------------------------------
/**
 * This is a very long description that should be wrapped because it exceeds the maximum line width
 * that is configured for the formatter to produce nice output.
 */
function longDescription() {}

/**
 * @param {string} name - This is a parameter with a very long description that should be wrapped
 *   because it exceeds the maximum line width.
 */
function longParamDesc(name) {}

/** Short. */
function shortDesc() {}

===================== End =====================
