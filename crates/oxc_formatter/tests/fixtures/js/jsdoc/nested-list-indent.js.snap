---
source: crates/oxc_formatter/tests/fixtures/mod.rs
---
==================== Input ====================
/**
 * Options:
 *
 * - First item with a long description that needs wrapping to multiple lines
 *   because it exceeds the print width.
 *   - Nested item also with long description that should wrap properly with
 *     correct continuation indent.
 * - Second item.
 */
const a = 1;

==================== Output ====================
-------------------------------
{ jsdoc: true, printWidth: 80 }
-------------------------------
/**
 * Options:
 *
 * - First item with a long description that needs wrapping to multiple lines
 *   because it exceeds the print width.
 *
 *   - Nested item also with long description that should wrap properly with correct
 *     continuation indent.
 * - Second item.
 */
const a = 1;

--------------------------------
{ jsdoc: true, printWidth: 100 }
--------------------------------
/**
 * Options:
 *
 * - First item with a long description that needs wrapping to multiple lines because it exceeds the
 *   print width.
 *
 *   - Nested item also with long description that should wrap properly with correct continuation
 *     indent.
 * - Second item.
 */
const a = 1;

===================== End =====================
