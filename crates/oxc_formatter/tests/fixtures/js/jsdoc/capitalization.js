/**
 * @param {string} name - the user name
 */
function greet(name) {}

/**
 * @returns {string} the greeting
 */
function hello() {}

/**
 * already Capitalized
 */
const x = 1;

/**
 * `backtick` should not capitalize
 */
const y = 2;

/**
 * @param {number} x - 123 starts with number
 */
function z(x) {}

// @type name should NOT be capitalized (it's a name, not description)
const current_role = /** @type {ARIARoleDefinitionKey} current_role */ (c_r);
/** @type {string | null} last Part of the filename */
const file_basename = undefined;
/** @type {Element} node */
const el = null;
