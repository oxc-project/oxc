expect(genCode(createVNodeCall(null, `"div"`, mockProps)))
  .toMatchInlineSnapshot(`
  `)

expect(genCode(createVNodeCall(null, `"div"`, mockProps)))
  .toMatchInlineSnapshot(`
  `).toMatchInlineSnapshot(`
    `)
