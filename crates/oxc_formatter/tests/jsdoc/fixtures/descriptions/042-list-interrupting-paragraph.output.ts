/**
 * Case A dash list, no blank line:
 *
 * - One
 * - Two
 */
export const a = 1;

/**
 * Case B star list, no blank line:
 *
 * - One
 * - Two
 */
export const b = 1;

/**
 * Case C ordered list, no blank line:
 *
 * 1. One
 * 2. Two
 */
export const c = 1;

/**
 * Case D plus list, no blank line:
 *
 * - One
 * - Two
 */
export const d = 1;

/**
 * Case E mixed markers, `1)` style then a plus item:
 *
 * 1. High priority items first
 *
 * - Low priority afterwards
 */
export const e = 1;
