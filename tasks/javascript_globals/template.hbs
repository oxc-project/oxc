use phf::{phf_map, Map};

pub static ENVIRONMENTS: Map<&'static str, Map<&'static str, bool>> = phf_map! {
{{#each envs }}
    "{{name}}" => phf_map! {
    {{#each vars}}
        "{{name}}" => {{writeable}},
    {{/each}}
    },
{{/each}}
};
