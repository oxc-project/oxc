[package]
name = "oxc_tasks_mcp"
version = "0.0.0"
edition.workspace = true
license.workspace = true
publish = false

[lints]
workspace = true

[lib]
test = false
doctest = false

[[bin]]
name = "oxc_tasks_mcp"
path = "src/main.rs"

[dependencies]
tokio = { workspace = true, features = ["full"] }
serde_json = { workspace = true }
serde = { workspace = true, features = ["derive"] }

# Optional: Add oxc dependencies if we want to integrate with oxc functionality
# oxc_allocator = { workspace = true }
# oxc_parser = { workspace = true }
# oxc_ast = { workspace = true }