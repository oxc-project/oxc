{
  "overrides": [
    {
      "files": ["**/*.custom"],
      "jsPlugins": ["./plugin.ts"],
      "languageOptions": {
        "parser": "./parser.mjs"
      },
      "rules": {
        "fix-plugin/no-var": "error"
      }
    }
  ]
}
