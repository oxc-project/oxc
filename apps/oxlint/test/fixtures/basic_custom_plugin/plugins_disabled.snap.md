# Exit code
1

# stdout
```
Failed to parse configuration file.

  x `plugins` config contains './plugin.js'. JS plugins are not supported without `--js-plugins` CLI option. Note: JS plugin support is experimental.

```
