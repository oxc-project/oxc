export default {
  meta: {
    name: "@e18e/eslint-plugin",
    namespace: "e18e",
  },
  rules: {
    rule: {
      create(context) {
        return {
          FunctionDeclaration(node) {
            context.report({
              message: `id: ${context.id}`,
              node,
            });
          },
        };
      },
    },
  },
};
