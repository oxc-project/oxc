# Exit code
1

# stdout
```
Failed to build configuration from <fixture>/oxlint.config.ts.

  x The `options.reportUnusedDisableDirectives` option is only supported in the root config, but it was found in an object provided via `extends` in `oxlint.config.ts`.
  | 
  | Move `options.reportUnusedDisableDirectives` to the root configuration file.
```

# stderr
```
```
