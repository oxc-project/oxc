# Input

## Command
```
oxfmt foo.json package.json tsconfig.dummy.json
```

## File tree
```
- fixtures/ <CWD>
  - foo.json
  - package.json
  - tsconfig.dummy.json
```

# Result

## Exit code
0

## stdout
```
Finished in <time> on 3 files using 1 threads.
```

## stderr
```

```

## File changes

### foo.json
`````diff
@@ -1,1 +1,1 @@
-{"x":1}
+{ "x": 1 }
`````

### package.json
`````diff
@@ -3,18 +3,19 @@
   "version": "0.0.0",
   "private": true,
   "scripts": {
     "build": "pnpm run build-napi-release && pnpm run build-js",
-
     "build-napi-release": "pnpm run build-napi --release --features allocator",
-
     "build-js": "node scripts/build.js",
-
     "test": "vitest --dir test run"
   },
-  "engines": { "node": "^20.19.0 || >=22.12.0" },
-  "dependencies": { "prettier": "3.7.4" },
+  "dependencies": {
+    "prettier": "3.7.4"
+  },
   "devDependencies": {
     "@types/node": "catalog:",
     "execa": "^9.6.0"
+  },
+  "engines": {
+    "node": "^20.19.0 || >=22.12.0"
   }
 }
`````

### tsconfig.dummy.json
`````diff
@@ -1,7 +1,7 @@
 {
   // Allow comments!
   "compilerOptions": {
-                 "module": "commonjs",
-                                             "strict": true,
-  },
+    "module": "commonjs",
+    "strict": true
+  }
 }
`````
