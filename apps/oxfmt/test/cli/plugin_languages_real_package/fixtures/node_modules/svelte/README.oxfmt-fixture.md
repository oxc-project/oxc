This fixture package provides a tiny CommonJS `svelte/compiler` implementation for the
real `prettier-plugin-svelte` formatter tests.

It only supports the narrow `.svelte` shapes used in this directory's fixtures and exists so the
real plugin package fixture can run without a workspace-wide Svelte install.
