This fixture package contains a CommonJS build of the real upstream `prettier-plugin-svelte`
source so Oxfmt can exercise package-name, override-scoped, and direct-plugin-object formatter
paths against real plugin behavior without depending on a workspace-wide npm install during tests.
